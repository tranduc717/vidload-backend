services:
  - type: web
    name: vidload-backend
    env: node
    buildCommand: npm install && pip install yt-dlp
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
```
